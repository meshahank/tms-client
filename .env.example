# Example environment variables for the TMS frontend
# Copy this file to `.env` and update values for your environment.

# API base URL used by the frontend (no trailing slash)
VITE_API_URL=http://localhost:5000/api
